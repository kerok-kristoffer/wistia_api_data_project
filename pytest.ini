# pytest.ini
[pytest]
testpaths = tests/unit tests/integration tests/contract
markers =
    contract: hits real external services; do not run in PR CI
addopts = -q --durations=10 --cov=ingestion --cov=transforms --cov-report=term-missing --cov-fail-under=0
